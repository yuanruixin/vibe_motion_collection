# AGENTS.md

> 本文件面向 AI 编程助手（Agents），说明本仓库的结构、约定与工作方式。修改代码前先读完本文件。

## 项目概述

一个**生成艺术 / 诗意动画**项目。技术 p5.js

## 目录结构

```
animation/
│
├── inspiration/                     # 灵感与设计参考（只读素材，勿随意改）
│   └── animation-inspiration.html   # 《生成动画灵感手册》：色板、技法、排版参考
│
├── motion/                          # 动画作品，每个作品一个编号目录。生成动画时默认放入此目录

```

## 核心约定

### 作品目录规范（`motion/NN-name/`）

- 新作按序号递增命名：`02-xxx/`、`03-yyy/`。
- 每个作品目录**必须包含两件套**：
  1. `index.html` — 单文件实现，CDN 引入依赖，浏览器直接打开可运行。

### 技术栈与硬性约束

- **p5.js 1.11.3**：`https://cdn.jsdelivr.net/npm/p5@1.11.3/lib/p5.min.js`
- **字体**：Google Fonts `Noto Serif SC`（中文衬线）+ `Cormorant Garamond` italic（英文）
- **画布**：全屏自适应，必须实现 `windowResized`；深色底基准 `#0a0e21`
- **无构建、无框架**：不引入 npm / bundler / 前端框架，保持单文件
  
### 动画制作要点（来自历史迭代教训）
- **画面比例**： 要求 9：16，竖版布局
- **时间参数联动**：幕长、诗句出现时刻 `t`、淡入淡出、粒子寿命、主体速度必须同步修改；只改幕长会导致叙事与诗句错位。
- **"必须完成的动作"用时间基准**：如落叶落地，用 `deltaTime` 映射保证在限定秒数内完成，而非物理速度。
- **循环闭环**：默认无限循环，总时长（当前 16.0s）各幕之和必须精确相等，末幕视觉需自然衔接回首幕。
- **性能**：粒子数按屏幕面积缩放，目标 60fps。
- **中文排版陷阱**：`letter-spacing` 会让末字后多空隙，用等量 `text-indent` 平衡。

## 工作流程

1. 先确保用户需求准确，不清楚的点列出，待用户明确后再开始实现
2. **改动画先改 `prompt.md`**：参数表是唯一事实来源，改完再同步实现到 `index.html`。
3. **本地预览**：`python3 -m http.server` 后浏览器打开对应 `index.html`。
4. **验收**：按该作品 `prompt.md` 的验收清单逐项核对（节奏采样、循环无跳变、缩放不错位等）。
5. 可复用的生成流程、排版技巧沉淀到 `.agents/skills/`。

## 禁止事项
